# Application Environment
NODE_ENV="development"
PORT="8081"

# GCP Project Configuration
GCP_PROJECT_ID="serverless-tek89"

# Google Cloud Credentials (for local development)
# Set this to the path of your service account key JSON file
GOOGLE_APPLICATION_CREDENTIALS="/path/to/your/service-account-key.json"

# In production (Cloud Run/Cloud Functions), Application Default Credentials (ADC) are used automatically
# No need to set GOOGLE_APPLICATION_CREDENTIALS in production

DISCORD_PUBLIC_KEY=
