# Database Configuration
# For local development with Cloud SQL Proxy (recommended):
DATABASE_URL="postgresql://pixelhub_user:YOUR_PASSWORD@127.0.0.1:5432/pixelhub"

# For production (Cloud Run/Cloud Functions) - uncomment and use this format:
# DATABASE_URL="postgresql://pixelhub_user:YOUR_PASSWORD@/pixelhub?host=/cloudsql/serverless-tek89:europe-west1:pixelhub-db-dev"

# Application Environment
NODE_ENV="development"
PORT="3000"

# Cloud SQL Instance Connection Name (get from: cd ../terraform && terraform output database_connection_name)
# INSTANCE_CONNECTION_NAME="serverless-tek89:europe-west1:pixelhub-db-dev"
