{
  "name": "firebase-auth-token",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "gcp-build": "tsc",
    "deploy": "gcloud functions deploy createFirebaseToken --runtime nodejs20 --trigger-http --allow-unauthenticated --entry-point=createFirebaseToken --region=europe-west1"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^4.0.1",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
