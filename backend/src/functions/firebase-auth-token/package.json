{
  "name": "firebase-auth-token",
  "version": "1.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "gcp-build": "tsc",
    "deploy": "pnpm build && gcloud run deploy firebase-auth-token --region=europe-west1 --source . --no-allow-unauthenticated"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^4.0.1",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
